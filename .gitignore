# Secrets — nooit committen
.env
*.pem
*.key

# Persoonlijke data — blijft lokaal
backups/
documents/
# viz-galerij: standalone demo's met geëxporteerde brein-data (labels/namen) — lokaal
viz-demos/
00. funcitonele omschreiving/
# eval-set bevat echte brein-content (namen, projectinfo) — lokaal houden
tests/eval_retrieval_set.json
scripts/brain_dump.json
# Metingen — geen bron (A1-telemetrie + A7-eval-rapporten in de repo-root data/);
# root-verankerd zodat src/span/evaluation/data/ (de wél-getrackte eval-set) blijft
/data/

# Python
.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/

# Tooling
.claude/
.claude-flow/
.mcp.json

# Rommel
None
str
list[str]
list[dict[str
tuple[Settings
hud/node_modules/
